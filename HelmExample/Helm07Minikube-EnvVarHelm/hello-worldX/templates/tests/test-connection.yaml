apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-worldX.fullname" . }}-test-connection"
  labels:
    {{- include "hello-worldX.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hello-worldX.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

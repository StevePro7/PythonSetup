

def update_query_set(context, kwargs, log_msg_prefix, extra_logger_data):
    context: dict = {
        "def": "ghi"
    }

    response: dict = {
        "wxy": "adi"
    }

    return context, response